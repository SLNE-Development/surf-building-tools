name: Publish to Maven & Create GitHub Release

on:
  push:
    branches:
      - version/**
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  build:
    uses: SLNE-Development/surf-workflows/.github/workflows/build-publish-release-gradle.yml@master
    with:
      modules: "surf-building-tools-*-all.jar"
      maven-publish: false
    secrets: inherit
